import React, { useState } from "react";
import LoginModal from "../../components/Modals/CustomerLoginModal";

const Home = () => {
  return (
    <div className="flex items-center justify-center w-full font-bold">
      hello
    </div>
  );
};

export default Home;
